FROM node:22-alpine

WORKDIR /app

# Install backend dependencies
COPY package.json package-lock.json* ./
RUN npm ci --omit=dev

# Copy backend source
COPY backend/ ./backend/

EXPOSE 4000

ENV NODE_ENV=production
ENV LOG_PRETTY=false

CMD ["node", "backend/gateway/server.js"]
